# Stage 4E frontend staging env — VPS only, chmod 600, never commit
# Generated from site.env with self-host API overrides by stage4e-frontend-staging-vps-api-smoke.sh

# Copy all non-Supabase keys from /opt/scholarshiptop/env/site.env, then override:
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=REQUIRED_LEGACY_EYJ_ANON_JWT_ON_VPS
SUPABASE_SERVICE_ROLE_KEY=REQUIRED_LEGACY_EYJ_SERVICE_JWT_ON_VPS

# Storage remains hybrid/hosted via other keys copied from site.env (if present).
