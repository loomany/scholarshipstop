/**
 * Top route progress (`nextjs-toploader` / nprogress).
 * Keep in sync with `styles/main.css` `#nprogress` overrides.
 * Matches Tailwind `orange-500` / focus rings on the site.
 */
export const NAVIGATION_PROGRESS_COLOR = '#f97316';

export const NAVIGATION_PROGRESS_SHADOW =
  '0 0 12px rgba(249, 115, 22, 0.4)';
