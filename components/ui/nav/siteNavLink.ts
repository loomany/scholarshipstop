/**
 * Shared class names for site navigation (see SiteNavLink.module.css).
 * Use clsx(siteNavLink.dark, active && siteNavLink.darkActive, …).
 */
import styles from './SiteNavLink.module.css';

export const siteNavLink = styles;
