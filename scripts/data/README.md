# Cached data files (optional / auto-download)

`world_universities_and_domains.json` is fetched automatically by `scripts/classify_hosts.ts` into this folder on first run (unless overridden by `HIPO_UNIVERSITIES_JSON_PATH`). Source: [Hipo/university-domains-list](https://github.com/Hipo/university-domains-list).
