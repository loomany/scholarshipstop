/** Final JPEG cover after sharp (16:9) — unchanged for on-site layout; Fal generates at FLUX_HERO_* then we resize/cover here. */
export const COVER_WIDTH = 1280;
export const COVER_HEIGHT = 720;
export const COVER_ASPECT_RATIO = `${COVER_WIDTH}:${COVER_HEIGHT}`;
export const COVER_ASPECT_RATIO_DECIMAL = COVER_WIDTH / COVER_HEIGHT;
